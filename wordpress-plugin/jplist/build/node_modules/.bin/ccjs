#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../closurecompiler/bin/ccjs" "$@"
  ret=$?
else 
  node  "$basedir/../closurecompiler/bin/ccjs" "$@"
  ret=$?
fi
exit $ret
